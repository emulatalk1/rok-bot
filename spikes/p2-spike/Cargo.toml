[package]
name = "p2-spike"
version = "0.0.1"
edition = "2021"
publish = false
description = "Throwaway: verifies P2a (off-screen window capture) and P2b (off-screen click acceptance) for rok-bot v0.1. Delete after P2 is resolved."

[dependencies]
xcap = "0.9"
enigo = "0.6"
image = "0.25"
anyhow = "1"
tracing = "0.1"
tracing-subscriber = "0.3"

[profile.release]
debug = true
