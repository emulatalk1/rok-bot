# macOS
.DS_Store

# Agent / IDE local state
.claude/settings.local.json
.omc/
.idea/
.vscode/
.gstack/

# Rust
/target
spikes/*/target
**/*.rs.bk
# Cargo.lock IS committed (binary crate — reproducible builds)

# Runtime artifacts. v0.1.4 captures pre + post per run as
# rok-capture-pre.png and rok-capture-post.png; the wildcard covers
# both plus any future runtime capture variants.
/rok-capture-*.png
